Use `Stat` for a single proof figure inside a `Card`.

```jsx
<Card padding="sm"><Stat label="Alunos ativos" value="1.248" delta="+12%" /></Card>
```

Numbers use the core typeface at 28px/600 with -0.03em tracking; the delta is the only place green and red appear.
