Use `IconTile` to seat an icon at the head of a card or list row.

```jsx
<IconTile><svg width="20" height="20">…</svg></IconTile>
```

Default is the raised ink square; `accent` (solid violet) marks the active item in a list, `quiet` is the violet-tinted variant used on glass.
