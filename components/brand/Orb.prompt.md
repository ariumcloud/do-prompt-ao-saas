Use `Orb` once per screen as the focal object — hero, or the final CTA section at a larger size.

```jsx
<Orb size={420} intensity="normal" />
<Orb size={560} intensity="dramatic" />
```

It is drawn in CSS, not an image file. If real 3D renders are supplied, swap the sphere layer for the render and keep the bloom.
