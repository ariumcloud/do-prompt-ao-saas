Use `Input` for e-mail capture and any single-line entry.

```jsx
<Input placeholder="seu@email.com" trailing={<Button size="sm">Entrar na lista</Button>} />
```

Placeholders are lowercase and literal; labels are short captions, not sentences.
