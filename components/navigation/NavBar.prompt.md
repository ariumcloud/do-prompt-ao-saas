Use `NavBar` at the top of any full page.

```jsx
<NavBar brand={<Wordmark />} links={[{ label: 'Módulos', href: '#modulos' }]} actions={<Button size="sm">Comprar</Button>} />
```

The link pill is glass; it is legible over the violet hero glow and over flat ink alike. Keep to five links.
